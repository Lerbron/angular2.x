/** * Created by admin on 2017/6/29. *//**   指令：分为三类，组件、结构型指令和属性型指令**   组件指令：拥有模版的指令（即：一般编写的组件）*   结构型指令：通过添加或移除dom元素来改变dom布局的指令。（如： *ngfor  *ngif)*   属性型指令：改变一个元素的饿外观或行为。***   属性型指令至少需要一个带@Directive装饰符的控制器类。**   @Directive装饰器需要一个css选择器，以便从模板中识别关联这个属性的html。用于属性型指令的选择器就是属性名称加方括号。[appBgDirective]* */import { Directive, ElementRef} from '@angular/core';@Directive({ selector: '[appBgDirective]'})export class BackgroundDirective {  constructor( public el: ElementRef) {    el.nativeElement.style.background = 'green';  }}